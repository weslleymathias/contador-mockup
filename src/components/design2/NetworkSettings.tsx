import { NetworkSettings as NetworkSettingsOriginal } from '../NetworkSettings';

// Reusing the original NetworkSettings component for Design 2
export function NetworkSettings() {
  return <NetworkSettingsOriginal />;
}
